# SvelteKit への移行計画

作成 2026-09-23。対象 `kuhaku.caprer.co.jp`。

## なぜやるか / やらない理由もここに書く

目的は**他サービス（LaneDuel 等）と構成を揃えること**の一点である。機能的な要求は無い。
このサイトは Worker スクリプトを持たない純静的配信で、動的要素は PostHog と
`<details>` の開閉だけ。SvelteKit の実行時機能を必要としていない。

外部レビュー（Codex）と実装側の見立ては一致して「**一貫性のためだけなら見合わない**」だった。
工数 6〜9日、うち大半は「保証が壊れていないことの証明」に消える。
そのうえで、1人で6サービスを保守する文脈切替コストを理由に**実施を決定**した。
この判断の前提が変わったら（例: 保守頻度がさらに下がる）、中断してよい。

**やらないこと**: 内容の追加検証。2026-09-23 に20件で凍結済み。移行は表示層の話であり、
判定・観測値・文面は一字も変えない。

## 不変条件（壊したら失敗）

| # | 不変条件 | 現在の担保 | 移行後の担保 |
|---|---|---|---|
| 1 | 第三者が **node 無しで**公開値を検算できる | `./run.sh verify`（Python標準ライブラリのみ） | **変更しない。** Python が証拠・算出・判定・公開ラベル・CSV/JSON を所有する |
| 2 | 配信される文面が検査を通っている | `lint_copy.py` が生成HTMLを読む | prerender 後のHTMLを読む。**検査した成果物をそのまま配信する**（再ビルドしない） |
| 3 | 「何を公開したか」の履歴が残る | 生成HTMLを git にコミット | 同左を維持する（ビルド成果物化しない）。README がこの目的を明記している |
| 4 | `.claude/` が公開されない | `site/public/.assetsignore` | **`parity.py --inventory`。** アダプタが `.assetsignore` を上書きするため除外設定には依存できない。配信物の一覧そのものを見る |
| 5 | `workers_dev:false` / 独自ドメイン | `site/wrangler.jsonc` | Worker 名 `kuhaku-zukan`、route `kuhaku.caprer.co.jp` custom_domain、zone `caprer.co.jp` を維持 |
| 6 | 主張が変わっていない | （無かった） | **`site/parity.py`**。移行前の指紋と比較する |
| 7 | 404 の挙動 | `not_found_handling: 404-page`（404.html は持たず Cloudflare 側が返している） | SPA の全捕捉にしない。**配信後に存在しないパスへ実際に HTTP 404 が返ることで確認**する |

## 安全網（先に用意した）

`site/parity.py` — 公開HTMLから「主張の指紋」を採る。

- 概念カードの数と id、判定クラス、カード内の全数値、改訂ID、体裁を除いた本文ハッシュ
- ページ全体の改訂ID・外部リンク・必須文言（正規表現で保持。表記ゆれで空振りした経緯あり）

検証済み: 概念削除・数値改変・改訂ID欠落・必須文言欠落・外部リンク欠落を**7件すべて検知**。
CSS差し替え・タグ種別変更・空白正規化では**誤検知ゼロ**。

基準は `site/parity-baseline.json`（移行前の版で採取済み）。

## 段階

### 第1段階 — データ契約の切り出し（完了）

`pipeline/viewmodel.py` → `data/viewmodel.json`。表示層はこれ**だけ**を読む。
公開ラベル・欠測と0の区別・訂正種別・採用基準ラベルは Python 側で解決済みで、
Svelte には解釈を持たせない。

`site/parity.py --contract` が「公開物の数値・改訂ID・概念がすべて契約から導けるか」を検査する。
**これが通ることが `build_site.py` を捨てる条件**。作成時に実際に3件の欠落が見つかった:
判定時に読んだ英語版の改訂ID（観測した版と違う）、判定ID、採用基準。

### 第2段階 — 足場（ほぼ完了・ビルドが1点未解決）

`web/` に SvelteKit + adapter-cloudflare 5.1.0 + Tailwind v4 + Biome + Vitest + pnpm。
`pnpm check` は 313ファイル・エラー0・警告0。表示層は `data/viewmodel.json` だけを読む
（`+page.server.ts` で事前生成時に一度読む）。

**LaneDuel との意図的な相違**: LaneDuel は Pages モード（`pages_build_output_dir`）だが、
空白図鑑は独自ドメインを wrangler の route で持っている。Pages にすると紐付けが設定ファイルの
外（ダッシュボード）に出て**不変条件⑤を壊す**ため、adapter-cloudflare のまま
**Workers モード**（`main` + `[assets]`）にした。

**判明した問題（不変条件④に直撃）**: adapter-cloudflare は出力先に**自前の `.assetsignore` を
生成する**（中身は `_worker.js` / `_routes.json` / `_headers` / `_redirects` の4つだけ）。
`.claude/` は含まれない。**`.assetsignore` に頼る方式は使えない。**
→ 配信物の**一覧そのものを検査**する方式に変える（第5段階）。

**ビルド確認済み**（本人許可のもと sandbox 外で実行）。実測した出力:

| 検査 | 結果 |
|---|---|
| 配信物 | **8ファイルのみ**（`index.html` / `404.html` / `_worker.js` / `_routes.json` / `_headers` / `.assetsignore` / `__data.json` / CSS 1件） |
| `.assetsignore` の中身 | `_worker.js` `_routes.json` `_headers` `_redirects` の**4項目のみ**。予測どおり `.claude/` は入らない |
| 404 | `404.html` = `Not Found` の実物。SPA 全捕捉ではない |
| 契約の疎通 | 事前生成HTMLに `schema 1.0.0 / run-20260908T120726Z / 全47件・判定済み20件・判定変更3件・根拠更新5件` |

途中で1つ壊した: `+page.server.ts` で `import.meta.url` からの相対パスで JSON を読んでいたが、
バンドル後は `.svelte-kit/output/server/` が基準になり `ENOENT` で prerender が 500。
**実行時に fs で読むのをやめ、Vite の別名でビルド時に取り込む**方式に変えた。
契約が変われば Vite が再ビルドするので、取り込み忘れも起きない。

### 移植中の検査体制（配線済み）

| 場所 | 何を見るか |
|---|---|
| `run.sh check`（ローカル＝CIと同一） | 証拠の完全性 / 公開CSV / 描画契約の完全性 / 配信物の一覧 / **主張の指紋** / 文面 |
| CI `verify` ジョブ | 上記＋「公開物が判定から再生成したものと一致するか」 |
| CI `web` ジョブ | `pnpm install --frozen-lockfile` → 型検査 → ビルド → 配信物の一覧 |

**第3段階の完了条件**: CI `web` ジョブに、事前生成HTMLへの `parity.py --check` を足して通ること。
いまは足場が仮ページなので通らない。これが通った時点で「主張を変えずに表示だけ作り直せた」
と言える。

型は**契約の JSON 自身から導く**こと。手書きの型を置くと Python が持つ意味づけが
二重化して、いずれずれる（`keyof typeof ...` でキーの union を取る形にしてある）。

### 第3段階 — 部品への移植と UI 修正（完了）

ページ全体を Svelte 部品に移植。`parity --check` で移行前と突き合わせ、差分を
**意図した変更だけ**に収束させた。

| 指標 | 結果 |
|---|---|
| 失われた数値 | **0件** |
| 増えた数値 | 18件（**全件が節数・ドメイン数**＝fix④で数値を出したため） |
| 概念数 / 改訂ID / 外部リンク | 20 / 36 / 40 で移行前と完全一致 |

基準は2つ持つ。`site/parity-baseline-prelaunch.json`（移行前）と
`site/parity-baseline.json`（移行後）。旧生成器の頁は前者と一字も違わないのが正しく、
新しいビルドは後者と一致する。CI の web ジョブが後者を検査する。

移植中に見つけて直したもの:
- **説明文の出どころを取り違えていた**。concepts 側が公開用、evidence 側は収集時点の
  内部メモ（★誤判定事例 等の目印が残り文面も古い）。11件が食い違っていた
- 検算結果の節が「2026-09-14 に実行した結果です」と日付を直書きしていて古くなっていた
- 看板の引用がカード生成ループに直書きされていた。契約へ移し、出典の改訂IDが
  判定の探索記録に無ければ build を落とすようにした
- 配布ファイルが表示層に無くプリレンダが 404 で落ちた。`run.sh build` で渡す

#### 旧メモ

作り直すので、UI の不具合はこの段で同時に直す（二度手間を避ける）。実測済みの8点:

| # | 問題 | 実測 | 対応 |
|---|---|---|---|
| 1 | 版面が広い | 47.5全角字/行（`palt` で実質さらに増） | 本文は約40字/行へ。表は別幅 |
| 2 | ライトモードの弱色 | `#6E6E75` on `#EFEDE7` = 4.32:1 | 4.5:1 以上へ |
| 3 | ~~意味を運ぶ罫線が薄い~~ **判定の序列が目で読めない** | **当初の指摘は誤り**。未検証の破線も斜線も `--kei` ではなく `--ai`(8.43:1) を使っており違反ではない。実際の問題は塗りが 12/26/22% で地との差が 1.24/1.62/1.49 と段差 0.2〜0.4 しかなく序列が読めないこと | 18% / 38% に広げ段差を1.38倍/1.51倍へ。文字色は `--ai`→`--ink`（ダーク38%塗りで3.75:1とAA未満になるため。`--ink`なら最悪6.56:1） |
| 4 | ドット符号化が数えられない | 折り返し＋60超で `…` 切り捨て | **数値を主、図を従に**。切り捨てで嘘をつかない |
| 5 | ダッシュ字形の混用 | 同じ「データなし」に `–` と `—` | 1種に統一 |
| 6 | 等幅の誤用 | 探索記録の日本語記事名が等幅 | 等幅は revid・英語タイトルなど識別子のみ |
| 7 | カード境界が弱い | 番号と余白のみ | 明示的な境界を与える |
| 8 | ヒーローの数字が直書き | `build_site.py:683` の「2件」 | 導出するか、語りの数字と明示する |

**残すもの**: 判定チップの塗りの濃さによる序列、色に頼らない符号化（斜線・破線）、
見出し明朝／本文ゴシック、単位に忠実な図示、「—は候補が無かった」の注記。
これらは情報設計として正しく、移行で失いやすい。

### 第4段階 — ゲートの作り直し（完了）

- **描画値の照合**（`lint_copy.check_rendered_values`）。`check_numbers()` は TOML の
  散文しか見ておらず、日英の列を入れ替えても素通りしていた。事前生成HTMLの観測表を
  概念×指標で契約と突き合わせる。欠測(—)と0も区別する。負のテストで検知を確認
- **`./run.sh web`**: データ生成 → 表示層ビルド → 配信物の一覧 → 主張の同一性 →
  文面と描画値 → 公開履歴の更新。**検査と配信の間で作り直さない**
- **公開履歴（不変条件③）**: ビルド成果物は git に入れないので、公開する版だけを
  `published/index.html` へ写して追跡する。旧構成の `site/public/index.html` と同じ役割。
  移行中は両方が存在する（site/public = いまの本番、published = 新ビルドが公開する版）。
  第5段階で配信を切り替えたら site/public は役目を終える

#### 旧メモ

- `lint_copy.py`: 位置引数の経路が数値照合・構造検査を飛ばす件は修正済み（`100adc9`）。
  prerender 後のHTMLを**完全な検査対象**として扱う経路を足す
- `check_numbers()` は TOML の文章しか見ていない。**描画された表の数値**を概念×指標で照合する
- CI の「再生成一致」検査は、HTMLが非追跡になると成立しない → **データ層の一致検査**に置き換え、
  HTMLは第3不変条件によりコミットし続ける
- 配信順序: Python の出力と検証 → フロント構築 → Python が最終HTMLとダウンロード物を検査 →
  **その検査済み成果物を配信**（配信時に再ビルドしない）

### 第5段階 — 配信と検証（1〜2日）

- 意図的に壊した文面・誤った描画値がゲートで落ちることを試験する
- 配信後: 公開すべきものが 200、`.claude/` 配下の**個別ファイル**と `.assetsignore` が非公開、
  配信内容が検査した成果物と一致すること（ディレクトリURLの404だけでは不十分）
- 失敗時の戻し手順を書く

## 進捗

- [x] 安全網 `site/parity.py` と基準の採取
- [x] `lint_copy` の黙殺経路と `run.sh` の配信後確認を修正（`100adc9`）
- [x] 第1段階 データ契約 — `pipeline/viewmodel.py` / `parity.py --contract` で完全性を検査
- [x] 第2段階 足場 — `pnpm check` 通過。`pnpm build` は sandbox の listen 制限で未実行
- [x] 第3段階 移植と UI 修正（UI修正8件すべて完了）
- [x] 第4段階 ゲート — 描画値の照合・`./run.sh web`・公開履歴の置き場所
- [ ] 第5段階 配信
